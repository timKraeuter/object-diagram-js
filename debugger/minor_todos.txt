Server side:
- Settings embedded external should immediately switch.

Client side:
- ELK Layout gets thrown around when new stuff is added (really hard to fix since elk is not build to do this).
- Evaluate/remove sentry
- Clean up unneeded features/code
- Fix testcases

Object diagram editing standalone (here we can add a watermark):
- drag append not working properly in chrome

Other:
- Minimap
